config:
  target: "http://localhost:8008"
  phases:
    - duration: 60
      arrivalRate: 100
  defaults:
    headers:
      Content-Type: application/json

scenarios:
  - name: "GET posts (before) 100rps 60s"
    flow:
      - get:
          url: "/api/posts"
