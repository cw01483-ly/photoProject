config:
  target: "http://localhost:8008"
  phases:
    - duration: 30
      arrivalRate: 80
  defaults:
    headers:
      Content-Type: application/json

scenarios:
  - name: "GET posts (before)"
    flow:
      - get:
          url: "/api/posts"